name: A1 GEN2 V5 W2W3 Regime Diagnostic V1
on:
  push:
    paths:
      - 'backend/research/architecture_factory/a1_gen2_v5_w2w3_regime_diag_v1.py'
      - '.github/workflows/a1_gen2_v5_w2w3_regime_diag_v1.yml'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m backend.research.architecture_factory.a1_gen2_v5_w2w3_regime_diag_v1
      - env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: ${{ github.run_id }}
        run: |
          python - <<'PY' > /tmp/body.md
          import json,os
          r=json.load(open('out/a1_gen2_v5_w2w3_regime_diag_v1.json'))
          print('<!-- A1_GEN2_V5_W2W3_REGIME_DIAG_V1 -->')
          print('run_id='+os.environ['RUN_ID'])
          print('W2='+json.dumps(r['W2'],sort_keys=True))
          print('W3='+json.dumps(r['W3'],sort_keys=True))
          print('receipt_sha256='+r['receipt_sha256'])
          PY
          gh issue comment 566 --repo "$GITHUB_REPOSITORY" --body-file /tmp/body.md
