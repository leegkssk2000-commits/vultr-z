name: R7A4D Strategy25 Single Loss Surgery Fast

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the grouped family indicator laboratory on this research branch.'
