name: R7A4D Vol Spike Base Fresh Holdout Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Vol-spike base fresh holdout completed and failed; workflow frozen.'
