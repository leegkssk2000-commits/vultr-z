name: ZEL Session Event Replay V1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantined:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'QUARANTINED_WRONG_ROUTE'
          exit 1
