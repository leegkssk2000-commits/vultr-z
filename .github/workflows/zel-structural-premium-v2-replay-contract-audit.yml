name: ZEL Structural Premium V2 Replay Contract Audit

on:
  push:
    branches: [master]
    paths:
      - ops/zel-structural-premium-v2-replay-contract-audit.sh
      - .github/workflows/zel-structural-premium-v2-replay-contract-audit.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: zel-structural-premium-v2-replay-contract-audit
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          persist-credentials: false
      - name: Prepare SSH
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key
          ssh-keyscan -H "$VPS_HOST" >> ~/.ssh/known_hosts
      - name: Run replay contract audit
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          set -euo pipefail
          scp -i ~/.ssh/vps_key -o BatchMode=yes \
            ops/zel-structural-premium-v2-replay-contract-audit.sh \
            "$VPS_USER@$VPS_HOST:/tmp/zel-structural-premium-v2-replay-contract-audit.sh"
          ssh -i ~/.ssh/vps_key -o BatchMode=yes "$VPS_USER@$VPS_HOST" \
            'bash /tmp/zel-structural-premium-v2-replay-contract-audit.sh'
      - name: Cleanup
        if: always()
        run: rm -f ~/.ssh/vps_key
