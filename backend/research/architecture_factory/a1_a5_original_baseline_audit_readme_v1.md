# A1 A5 Original Baseline Audit V1

This lane exists because a derived Trend Rider cohort was previously compared as if it were the canonical original fresh cohort. The same class of lineage error must not silently affect the other A5 finalists.

## Authoritative rule

- Trend Rider numeric improvement claims must compare against `ORIGINAL_TREND_RIDER_FRESH_W1_W2_W3`.
- `break_and_continue`, `supertrend_pullback`, `keltner_trend`, and `trend_ma_macd` must first compare exact-parent challengers against their frozen original GEN1 screening baselines.
- A generic LLM-generated executable is scout material until bound to the exact original parent.
- Provider/parser/timeout failure is technical failure, not economic failure.
- A positive screening-cohort A/B creates a fresh challenger only; it does not increment A1/A2/A3 PASS counts.

## Immediate retest order

1. `break_and_continue` — existing exact relative-volume adapter first.
2. `keltner_trend` — band-expansion transition, then relative volume.
3. `supertrend_pullback` — trend persistence, then volatility regime.
4. `trend_ma_macd` — redundant-component ablation, then multispeed trend.

All axes remain one-at-a-time, no threshold sweep, no holdout access, no post-outcome trade deletion, and no execution/order/live authority.
