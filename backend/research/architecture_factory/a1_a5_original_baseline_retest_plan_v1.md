# A1 A5 Original Baseline Retest Plan V1

The old A5 zero-ready receipt must not be interpreted as an economic failure because the run produced zero candidate/economic rows while both paid providers failed at transport/JSON parsing.

The retest is therefore fail-closed and lineage-first:

- Trend Rider: continue the direct same-original-fresh W123 A/B lane only.
- Break and Continue: consume the existing exact parent relative-volume adapter first.
- Keltner Trend: build exact-parent band-expansion transition child before any generic generated executable is considered.
- Supertrend Pullback: build exact-parent trend-persistence child; retain the six-trade perfect GEN1 result only as a screening reference.
- Trend MA MACD: build exact-parent one-component ablation child; retain the three-trade GEN1 result only as a screening reference.

A challenger may advance to fresh testing only if it preserves the declared original parent, changes one causal axis, uses the frozen cost model, and emits a deterministic parent/child receipt. No old generic FAIL/HOLD closes these retests.
