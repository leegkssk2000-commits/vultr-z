{
  "name": "z-os-app-canonical-source",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical source root for app.z-os.vip. Baseline-preserving source scaffold created from current live dist.",
  "scripts": {
    "build": "bash scripts/build_from_baseline.sh",
    "validate": "bash scripts/validate.sh",
    "deploy": "bash scripts/deploy_locked.sh",
    "status": "bash scripts/status.sh"
  },
  "zOps": {
    "role": "canonical-app-source-root",
    "liveHost": "https://app.z-os.vip",
    "deployRoot": "/var/www/z-os-app",
    "baselineOnly": true,
    "rule": "Patch/build/deploy only from this root. Never use z-os-pwa, z-os-alimi, backup, restore, dist, or node_modules as source roots."
  }
}
