# PR1221 review3961173071 disposition — no economic replay

Accepted P1: the old verifier authenticated both archives but did not independently link normalized rows to raw FT trades. Added raw_link.py with no engine, strategy, or original accounting imports. It parses the raw archive, enforces a one-to-one pair/open-time identity (order-independent), independently derives calendar/entry/exit prices, force_exit versus terminal mark, intrabar time bounds, common costs/funding/floor and separate FT accounting, and compares these with every normalized row and native summary.

The compact NORMALIZATION_CONTEXT.json was derived from the original artifact10070956079 SEEN2026.json.gz, whose exact bytes match the pre-execution SPEC input_file_sha256. It includes the original seven-symbol cost binding and seven final completed closes. Its provenance is that executed artifact; it is not a new market query, actual fills, or an independent dataset. The cost mapping digest is checked against the frozen SPEC. A separately pinned REVIEW_HASHES.json authenticates the new context and raw verifier. The original21-file FINAL_HASHES.json and both old verifier/test files remain unchanged; verify_raw.py invokes the existing verifier first, then requires the new raw linkage.

All62 actual raw trades (D2=36, hlhb=26) match the independent reconstruction. All original RESULT/RAW_ENGINE/RECEIPT/SPEC/BUDGET/strategy/source artifacts and REPORT.md remain byte-identical. The economic figures and verdicts did not change. New tests include an internally consistent fabricated normalized profit, missing/duplicated/reordered raw rows, altered clocks/symbol/reason, forced fills versus terminal marks, intrabar cost bounds, native-fee substitution and altered source costs. Existing saved verifier and relocation tests remain. This closes a verification omission; no actual report mismatch was found, no original strategy reran, no trial budget was reset.

The historical temporary-path helpers remain sealed in PR1220. This successor continues to use the portable relative-path entrypoint. The workflow stays read-only, with no engine install, market input fetch, replay dispatch, remote writer, or deployment. Final scoped CI, review disposition, merge and exact-merge saved verification are recorded in the PR completion comment.
