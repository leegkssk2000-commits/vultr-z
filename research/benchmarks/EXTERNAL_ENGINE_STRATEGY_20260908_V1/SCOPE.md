# External engine and strategy benchmark — active successor, no performance claim

Explicit user instruction: benchmark engine and external strategy rather than another D child. Current base a4171ae85d06cbf9947185ac13d306f4331a2d0b. Preserve exact KR3/D/D2 and47 hypotheses/68 evaluations, all source/OOS/API budgets, other collectors and old scopes. This is scoped research code and related CI, explicitly requested under AGENTS. No deployment, SSH, live exchange, market collection, paid AI or unused OOS access.

First independent-engine target: stored Break V2 (4h, volume-confirmed previous50-bar high breakout, bounded EMA20/50, H6), because its minimal lifecycle isolates feature/timing/ownership differences. D/D2 are not silently replaced as development parents. The Freqtrade port's native NEXT_OPEN exits may differ from ZEL's prespecified HELD_CLOSE fills; these differences must be reported and never forced into false parity.

Engine: Freqtrade2026.8 actual Backtesting, long-only price-path analysis with externally supplied analytical market constraints. This is not a BingX futures connector certification. Raw futures OHLCV is input; leverage/size/account performance are not introduced. Normalize gross price returns and apply the inherited research costs independently; do not add Freqtrade profit and fees a second time. Stock engine behavior and strategy overrides must be documented.

External reference selected before new outcomes: Heracles.py from official freqtrade-strategies, exact file blob b69656b76ee142c0945dbb0229a19d67d5516215 / source commit7f91ff52bb664423ae673092a6a18d76c50c2c29. Reason: native4h,long-only,complete entry/ROI/SL rules; NOT advertised past return or optimized against ZEL. Its original source recommends100day listing age, has25.6% stoploss and time-decaying ROI. Preserve all parameters including loaded buy_params (shift15, not class default16); no hyperopt. Historical listing-age handling and missing intrabar paths must be explicit before any market evaluation. Source comments advertising old returns are not evidence for this benchmark.

Initial PR is synthetic setup only: no market rows, no new candidate or evaluation consumed. After smoke and frozen comparison contract, at most one port and one external reference over each of two already-used periods (4 benchmark applications total) may execute. A real attempt and canonical counter reservation must be durable before each application. Identical completed results are reused, never rerun. Runtime failures remain counted/recorded if evaluation began. All economic outputs, including failure, must remain saved.

First deliverables: actual engine mismatch list (first signal/entry/exit/price/cost/state divergence), actual external rule results under clearly stated cost/terminal/age conventions, and a reuse/replace/not-supported assessment. Independent implementation is NOT independent OOS. Do not install a new platform/daemon or add candidate sweeps. Stop at finite completion/checkpoint; no waiting for G5 PASS.
