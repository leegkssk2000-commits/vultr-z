[CONTINUE — G4 원래25개 원문·코드·경제비교·역할 선별]
repo=leegkssk2000-commits/vultr-z
scope_key=G4_EXACT25_SOURCE_TO_CODE_IMPLEMENTATION_AFTER_FINAL_REVIEW_V1
작업 대상은 원래25개이며 Scalp7의 MR/Micro 추가 lane으로 분모를 채우지 않는다.
이 지시는 구현·시험·기존 경로 통합을 실제 진행하는 지시다. 일반적인 재벤치마킹 계획서로 끝내지 마라.
Work를 구현·경제실행의 단일 담당으로 유지한다. 별도 Chat 경제실험을 병행하지 않는다.

[회수할 결과와 자료]
branch=codex/g4-25-implementation-preflight-20260920
server_worktree=/home/z/worktrees/g4-25-implementation-preflight-20260920
base_commit=101fe5ff8fe181477489758e098243fd4a87890c
본 디렉터리의 RECEIPT.json과 EXACT25_BINDING.json을 읽고 실제 파일/해시를 확인한다.
첨부 G4_25_FINAL_REVIEW_PACKAGE_20260920.zip의 최종 정정과 그 안의 R1/R2/R3 원문 대조 자료를 사용한다.
BB III의 일괄 당일 매매금지는 삭제된 조건이다. Raschke Anti는 선행 추진을 가진 별도 재개 모드다.
수익 주장·원형 대응·현재 코인15m/30m 경제성·재료 기여를 별도 상태로 유지한다.

[이미 구현된 범위 — 완료를 부풀리지 않는다]
scalp7_implementation_contract_v1.py의 validate_rules는 규칙별 근거 형식을 검증한다. 원문 진실/코드 동일성을 인증하지 않는다.
inspect_entry_bar는 한 세부봉의 진입 가능/불확실 상태를 판정하는 함수다. 전체 Sub-bar Replay 또는 실제 체결 증거가 아니다.
value_account_snapshots는 동시각 잔여수량·가격·누적비용으로 평가한다. 실제 MARK 곡선을 수집/생성한 것은 아니다.
58개 신규 인공시험을 포함한 Scalp7 1001개 시험 및 기존 저장검증 7개를 실행했다. 경제실행·새 원형재현은 0회다.
기존 execution_v2/metrics_v2/campaign_v2 및 동결 전략은 변경하지 않았다. 신규 함수는 기존 캠페인에 아직 연결하지 않았다.
EXACT25_BINDING의 경로는 텍스트 검색 결과이며 호출 경로 검증이 아니다. false 상태는 이번 증분의 미실행이고 과거 판정 삭제가 아니다.

[시작과 충돌 방지]
최신 origin/master, 현재 branch/PR, 작업 잠금·실행 예약·이미 끝난 후보를 먼저 확인한다.
/home/z/z 운영 checkout의 기존 미커밋 변경은 건드리지 않는다. 정리·reset·서비스 재시작을 하지 않는다.
PR1340/1341 및 이후 유효 결과를 보존하고 같은 조건의 완료 실험은 재사용한다.

[이어갈 실제 구현]
1. 원래25개 각각의 최종 자료 행을 실제 signal 생성 함수·입력·청산 callback·기존 시험과 대조한다.
원 모드/버전/시장/TF, 실행 가능한 조건식·단위·초기화·상태순서를 규칙별로 동결한다.
SOURCE_DIRECT에는 근거 문단/사례, EXISTING_FROZEN에는 코드/해시, DECLARED_HYPOTHESIS에는 이유와 별도 ID가 있어야 한다.
누락 수치를 관행적 BB/ATR/SL/TP 기본값으로 조용히 채우지 않는다. 원문이 없는 고정 TP를 모든 전략에 강제하지 않는다.
명세에 가설이라고 적는 것만으로 충분하지 않다. 경제결과를 보기 전 원 사례·비진입·실패 사례와 실제 함수를 대조한다.

2. 필요한 원형부터 기존 generate_signals/entry_update/exit_update와 연결한다.
새 BaseStrategy나 지표 계산을 execution_v2 안에 추가하는 전면 개편은 하지 않는다.
조건부 주문이 필요한 경우 작은 버전별 adapter에서 주문 활성화·취소·잔여수량과 세부봉 연속성을 유지한다.
inspect_entry_bar의 후보 상태를 관측 체결로 승격하지 말고 선택한 연구용 fill 모델의 가정·비용·시간 정밀도를 별도 기록한다.
같은 1분의 진입/stop 순서가 모호하면 사전 고정 불확실 처리; 한 경로를 사실처럼 강제하지 않는다.
전략 결정·trailing 갱신 TF는 15m/30m를 유지한다. 체결 해상도 보완으로 1분 전략을 새로 만들지 않는다.
기존 next-open 기준은 보존하며 새 execution profile의 결과를 과거 결과에 덮어쓰지 않는다.

3. 실제 실행 원장에서 동시각 계좌 snapshot adapter를 연결한다.
잔여 실제수량·가중평균진입가·실현 gross·수수료·signed funding을 분리한다. 레버리지 재곱셈·슬리피지 이중차감 금지.
MARK_PRICE, LAST_PRICE proxy, 봉 극값 stress를 분리한다. mark 미확보를 low/high로 채워 실측 MTM이라고 부르지 않는다.
value_account_snapshots는 외부 입출금을 지원하지 않는다. 입출금이 있다면 기존 현금흐름 조정기를 검수해 쓰거나 미지원으로 남긴다.
표본시각 DD를 intrabar 최대 DD나 청산 시뮬레이션으로 보고하지 않는다.

4. 각 전략에 출처→조건식→실제 caller→체결→손익 시험을 붙여 승인 범위의 전체 시간순 경제비교까지 연결한다.
첫 완료 비교는 T/WR/Gross/Cost/Net/Net-T/PF/DD/연패/미완결/승리훼손/기회누락으로 즉시 보고한다.
재료는 허용된 동일 host/host+component 비교로 역할 기여를 검증한다. standalone 실패만으로 폐기하지 않는다.
25개 중 준비된 항목은 진행하고 막힌 항목만 분리한다. 쉬운 지표 세 개만 반복해 전체 작업을 대신하지 않는다.

[예산과 완료 기준]
구현·시험을 다시 승인받느라 기다리지 않는다. 새 FULL은 실제 승인 원장과 identity·상한을 대조한 뒤 실행한다.
PR1341의4회는 모두 COMPLETED이며 재사용 가능한 잔여 예산이 아니다.
새 FULL 예산이 없으면 원래25개 중 실제 준비된 미완료의 신규 기준본·필요 대조군·최소 실행 수를 한 번에 산정한다.
그 경우에도 실행권한과 무관한 명세·코드·인공시험·저장결과 검산은 계속 수행한다. 옛 초안 문구로 전체 중단하지 않는다.
이 파일은 새 유료 지출·임의 경제실행 횟수·서비스 변경·LIVE·배포·승격을 발급하지 않는다.

결과 보고는 변경 파일/함수, 실제 시험과 미실행, 원래25개 중 닫힌 공백, 새 경제표 또는 구체적 장애를 구분한다.
이전 broad/fidelity/economic 저장검증을 보존하고 관련 CI·정상 PR 리뷰를 수행한다.
커밋/PR 저장은 해당 작업을 백그라운드 Work 세션에서 자동 실행했다는 뜻이 아니다.
자료만 재정리하거나 새 테스트 수만 보고 G4 경제개발 완료라고 하지 않는다.
