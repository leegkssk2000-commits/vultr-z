# C63 reference vs pre-release squeeze pullback entry

User `다음 들어가` approves the preceding bounded shift from repeated exits to one entry-architecture experiment. This is NEW scope C63_PRE_RELEASE_PULLBACK_ENTRY_V1, at most1candidate and2 first FULL period applications. C63 is unchanged comparison, not a secretly modified parent. No FIXED view is claimed: entry times/prices/opportunity pool change. No extra parameter/feature/symbol/year variants. On rejection pause this C63 research branch rather than cycling back through exits. This is a work-priority decision, not a new formal gate or deletion of C63.

Start master717390a76c8808e181a9ae8fa45708cbe1630b7c, current/recent PRs and same-method search read once. PR1240 is the previous pivot experiment, not this entry design; preserve its existing actual results67/118 and imported local one-floor-distance history. It remains independently pending merge. Its exacthead832bf59d9d4c183f2bf8e88d437d75e91867a10a budget is inherited byte-for-byte as HISTORY_PRIOR, before appending68/119-120 on actual first starts. Remote master still has older budget branches; do not falsely say all historical results are merged. Coordination comment5608750772. Single owner this Chat, no separate Work launched. Local clone failedDNS, local source is partial scratch; `frontend/z-os-app-source missing in this checkout.` No local fullcheckout/AGENTS validation PASS. Explicitly approved backend research, tests, relatedCI only. Remote full checkout must perform canonical frontend pre-check BEFORE freeze or economics. No SSH/deploy.

## Source basis vs this ZEL adaptation (before any economics)
https://www.simplertrading.com/trading-plan (read2026-09-09) describes preparing around EMA21 while a squeeze forms. It also discusses higher-timeframe/market context and defined risk. This public educational description is not audited strategy performance.
PR1238's preserved source-timing contract, `CREATOR_TIMING_CONFORMANCE_AFTER_PR1237_V1/DESIGN.md`, records the separate S1 options plan's3daily squeeze observations and EMA21 entry context, with qualitative/fill gaps. We do NOT claim its proprietary Squeeze Pro, cup/handle/high selection, options PnL, daily session timing, or full management plan is reproduced. No full creator-trade case is fabricated. This test isolates the early-entry mechanism on the existing4h instruments;3native bars is an explicit ZEL adaptation, NOT3source trading days. Public return claims are not copied as our outcome.

### Concrete new execution contract
Use unchanged native BB20/KC20 squeeze and14bar price momentum. Compute EMA21 from the same close prefix with native SMA seed and alpha2/22, not a future backfilled average. Each contiguous squeeze episode has at most one prepared opportunity. After3or more completed squeezed bars, the first close strictly above EMA21 with positive native momentum and episode minimum low below EMA21 fixes the preparation level and floor. These reference prices never move while waiting. Only a LATER still-squeezed bar whose low touches/breaches that level and whose close strictly recovers above it with positive momentum emits entry. Floor close invalidation or first non-squeeze close cancels preparation; no rearm within that episode. All observed episodes, including failed/no-touch/no-signal/occupied ones, remain logged. Do NOT preselect episodes based on later C63 success/release.

The4h source has no timestamped passive-limit fills/queue evidence. Therefore this first adaptation uses COMPLETED PULLBACK CONFIRMATION -> NEXT ACTUAL OPEN, not an assumed fill at the EMA level or candle low. This changes the previously sketched pending-limit implementation explicitly; it does not claim the original passive order was replicated or price necessarily improved. Native next-open gap-floor invalidation still applies. A release on the confirmation bar cancels rather than granting a backdated earlier entry. Preparation before evalstart is not carried in, though indicator/episode history is fully warmed from the original prefix. No new key/AI/market request is needed.

Initial floor is fixed using data known at PREPARATION; never use the later C63 release candle's lower low. Native M1 close-floor / nonpositive14momentum /20heldbars -> next actualopen exit is reused unchanged. Holding starts at this new actual entry, no free pre-entry funding or extended horizon. Native same-open exit/reentry occupancy policy retained, no partials, scale-in, averaging-down, residentSL or live orders. One full initial reference notional per admitted trade; identical modeling cost floor20bps/absolute8hfunding and cost2. No leverage or risk normalization games. Same existing seven instruments and exact used2025/2026 windows/input hashes. Do not read source after approved terminal Sep5.

## Comparison and interpretation
Direct report C63/ENTRY: positions, WR, averagewin/loss, PF/payoff, terminal net/cost2, daily markedDD, exposure/streak and open marks. Eight old strict goals (WR/net/cost2 up andDD down EACH period) retain numeric equality policy. Positivity, practical materiality and dependence are separately stated; goalcount alone is not economic adoption. Shared SQUEEZE EPISODE joins are retrospective attribution, not identical trades or fixed-fill experiments. Match by symbol+original episode start; sum changed common CC/CO/OC/OO + removed + new to total delta. Report entry-time/price differences and new failed early entries, plus profitable later C63 breaks missed by this approach. Original winner/topdecile profit capped retention fixed before reading results. Do not add avoided-loss or saved-fee effects twice or count no-fill as a win. Same-calendar marked risk, largest-trade/symbol dependence reported. No independent/OOS/G5 credit; reused DEV selection remains.

## Finite completion
20new synthetic tests pass locally against exported native code; remote existing/new tests required. One frozen SPEC and immutable input/dependency hashes, remote budget reservation and readback precede each engine start. Persist consumed failures; no retries/automatic successor. Parent results reused, no parent replay. Two FULL applications119/120 only. Whole pipeline uses standard existing GitHub checkout/runner; provider keys EMPTY. After first results replace economic workflow with saved validation only, do required finalCI/review/normalmerge/exactmerge savedcheck. If time/capability blocks, CHECKPOINTED with real IDs, not fake completion. Max45controlled minutes; no secondary reviewer, API summary or master chasing after last required result. C63/M1/C54 andC64-C67rejections plusAVWAP6unused slots/Q0/G5B/other collectors preserved. API previous failed reservation remains blocked and untouched.
