# C70_TM_CAPREUSE_V1 — frozen before economics

Issue1261 explicitly authorizes this successor. The closed PR1260 scope remains REPORT_ONLY. This is ZEL_OCCUPANCY_REPAIR, not a Qullamaggie original rule or a new management variant. Original registered71 signals/context/next-open safety and PR1260 unit lifecycle functions are imported unchanged. C63/C63_TM are never replayed. Parent results are read from hash-bound saved ledgers.

One full-entry reference quantity per symbol is 1.0. Every campaign carries an exact rational allocation. Actual partial fills release allocation/3; actual final fills release the residual. Pending decisions and terminal marks release nothing. At equal timestamps we retain the original conservative close-before-next-open ordering: size/reserve from fills strictly earlier than the signal close; then observe old next-open fills; then enter the reserved amount at that actual open. Equal-open releases do not upsize that order. Original gap/floor/window checks remain mandatory. No OHLC path is inferred.

The native research simulator has no exchange minimum lot/notional model. Its existing minimum is positive normalized quantity. Exact fractions preserve this without an arbitrary epsilon or fitted overlay limit. This does not assert real-exchange minimum-size or same-side multi-lot production compatibility.

Each admitted signal calls the exact frozen unit manager. Independently computed lifecycle paths can be merged because campaigns do not change one another's prices or source rules. Admission reads only prior actual fills from those paths, never future exits, marks or PnL. Prefix mutation and truncation tests cover admission, quantities and trace. Future COMPLETED/CENSORED reporting is not a decision feature.

Raw manager qty remains local campaign unit1, while allocation_numerator/denominator and normalized trace fields bind it to the pool. Monetary and exposure reports multiply every leg by the allocated reference quantity. Fee/spread/impact/funding/frozen cost-floor reserve apply to each weighted leg, including terminal hypothetical liquidation. WR counts one fully completed signal campaign; partial legs add no WR observations. Average win/loss, PF and terminal bps use original full-reference contributions, not renormalized small-lot returns, account returns or realized equity percentages. MFE/MAE retain the original unscaled underlying-path meaning.

Diagnostics inspect all saved TM occupancy exclusions, including signals rejected by entry eligibility and signals absent from baseline. This does not preselect profitable blocked trades. Every eligible later signal with positive released capacity is admitted at its observable next open subject to original safety. Same rules apply in both USED_DEV windows.

Economic budget: candidate82 only, evaluations147/148 (DEV2025/SEEN2026), first FULL once each. No parent/FIXED/sweep/retry/new-OOS/paid-AI/orders/deploy. Consume on actual start, including failures. No automatic restart. Source preservation and occupancy correctness are separate from economic acceptance. Preserve existing both-period positive net/cost2 requirement and cumulative DD/ordinary/top-decile retention gates without relaxation; expose WR and risk deterioration. Neither economic acceptance nor code merge grants live/G5 promotion.

Complete source, budget, inputs, diagnostics and tests are frozen and read back before FULL. CI executes synthetic tests and saved verification only. The temporary first-FULL dispatcher is removed after execution. A/B/C bridges compare baseline→TM, TM→repair and baseline→repair. New restored baseline campaign returns are explicitly quantity weighted; the prior 2026 -1725.68bps occupancy diagnostic is not assumed recoverable.
