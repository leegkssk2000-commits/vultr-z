# Existing review resolution before first model request

P1 review3968061577: manual issue_comment previously checked ancestry of approved merge but checked out current master. Resolve trusted PR metadata without checking out repository code, reject a changed workflow blob between GITHUB_WORKFLOW_SHA and that reviewed merge, then checkout EXACT merge_commit_sha. Provider secrets are now supplied only to the final request step, not the bootstrap, checkout or offline preflight. The reviewed execution boundary repeats HEAD==approved merge equality, not ancestry. A regression rejects a newer master even if the old merge is an ancestor. Current/default-branch YAML is never silently accepted when different from the merged reviewed YAML.

P2 review3968061584: the original JSON decoder checked only required keys. The sole manual execution entrypoint creator_squeeze_reviewed_v1 wraps that decoder and validates list/object shapes, nested strings and known source IDs, status enum, boolean flags and string-array unresolved/tests/limits BEFORE it can return RESPONSE_COMPLETE. Prompt specifies those shapes. Sixteen offline regressions cover the concrete malformed results and boundary restoration; prior30tests retained. The original helper remains available for no-network legacy unit tests, not as a second manual provider entrypoint.

Independent continuation5601454633 correctly noted supplied code scope is only squeeze_features and exit_reason. The model prompt now explicitly limits its conformance claim to those two functions; a hash of complete files is not full-code inspection. Source S1's actual runner clause correction is in SOURCE_CORRECTION and current SOURCE_CONTRACT. No paid or economic runtime was launched before these repairs. No broader automated caller, provider retry or new model is added.
