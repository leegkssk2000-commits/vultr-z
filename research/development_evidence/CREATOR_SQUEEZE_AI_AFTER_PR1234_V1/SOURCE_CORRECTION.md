# Pre-call source scope correction

Before any source/API runtime or reservation, a more complete extraction of S1 exposed explicit remaining-third management: a prior-three-trading-day low trail, final1.618 objective and optional exit at1.272. The initial SOURCE_CONTRACT incorrectly called the whole residual exit unspecified; it is now QUALITATIVE only for the unspecified offset and discretionary branch choice. S1 also explicitly explains initial2ATR below underlying entry and later1ATR. S2's daily2ATR-band wording is kept distinct.

The initial S1 extraction endpoint at Classes would also include the unrelated PivotPoints plan. The executable selector now stops at Trading Strategy2. S2 now covers only entry/stop/exit/risk before the trade-journal section, reducing unrelated text while retaining the actual original passages given to both providers. Gemini is asked to return compact<=3000UTF8byte JSON so the second role can receive the full extraction plus the same source/code under the unchanged16KB input ceiling. Missing original-source fields are not filled by these corrections.

DESIGN's original boundary/unspecified-residual prose is superseded by this note and current SOURCE_CONTRACT; initial draft remains in history. Thirty original local offline tests pass on the corrected module; an additional local boundary regression passes (31total locally). Routine remote workflow currently runs the30committed tests; do not claim31remote tests unless that test is separately committed. Source/code/API changes precede first paid request. No new strategy, price evaluation or financial performance claim.
