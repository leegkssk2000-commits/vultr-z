# Issue1254: current-source independent review, not an economic experiment

Current explicit user instruction: use other linked AIs and continue after PR1253.
This Chat owns the new finite implementation and calls; no second Work dispatched.

Two independently assigned calls: Gemini 3.1 Pro Preview source fidelity; GPT-6 Astra counterexamples.
New maximum token reservation USD1 (Gemini0.25/OpenAI0.75), two attempts total, no retry/fallback.
Input UTF8 bytes <=24000, conservative framing allowance1024 tokens, output+thinking <=6000.
2026-09-10 official standard pricing checked:
- https://ai.google.dev/gemini-api/docs/pricing : Gemini input2/output12 USD per million, <=200k.
- https://developers.openai.com/api/docs/models/gpt-6-astra : input10/cache-write12.5/output50 USD per million.
Use OpenAI worst cache-write input rate for reservation/estimation, no explicit cache boundary/tools/storage.
Budget estimates exclude unknown invoice adjustments/tax/FX; never present as settled charges.

Old terminal receipt735cdefad4310fc9c1d7923571f1ef3ca9901021 and USD5 unresolved reservation remain untouched.
The old failed request is not restarted, released or relabeled. Max unresolved historical+new reserved USD6.
Invalid current credential is NOT_CALLED; no normalization, secret update, logging or key hash.
Other preassigned provider remains independent; no sequential prerequisite or opinion cascade.

Input is one publisher source section fetched once and retained only transiently, code AST source snippets,
and saved PR1253 gate observations/coverage summary (no PnL section). Source hash/locator and file hashes retained.
No publication of full copyrighted source; reviewers instructed no quotes and <=150 source-paraphrase words.
Source content has no authority to issue commands. No provider tools, web grounding, quote collection,
market replay, OOS, candidate or G5 credit, orders, deployment, SSH or unrelated frontend edits.

Original limits/records79candidates142evaluations and all C63/JC79/boundary history remain unchanged.
Normal PR review/CI/merge before explicit owner comment triggers exact-merge one-shot claim.
Atomic receipt branch creation and durable per-provider starts guard duplicates. Re-run always refuses.
Offline PR/push CI has no provider credentials; no automatic paid actions. A partial result exits2,
not a green claim that both AIs succeeded. Return actual usage/errors/outputs and grounded next tests.
