# Keltner M — causal D opportunity reservation, ordinal30

P is the original control. D is the unadopted PR1196 EMA-invalidated exit workcopy. N is the unadopted PR1197 upper-half signal admission workcopy (2025 TRADEOFF, seen2026 REJECT). M keeps every N entry and D exit numerical rule and changes only per-symbol opportunity ownership after a rejected entry. None of P/D/N's files or verdicts are rewritten.

## Existing evidence and selection bias

PR1197 full N closed net2025+4556.3060 and prior N_COMMON_D202+6022.2077 are already seen results. The known difference+1465.9017 motivated M. It is not new independent evidence or an M measurement. Existing D15 direct half-bar vetoes link to N10 replacement entries and2 further displaced D entries. Nine new entries follow directly rejected D intervals; the LINK chain D2114 veto→N2120→D2129 displacement→N2138→D2145 displacement demonstrates propagated ownership effects. Restoring the2 displaced D losses costs497.3065, so removing the new10 net loss1963.2082 alone overstates expected recovery. Full N and COMMON_D are already equal in seen2026; no profit recovery there is promised.

The offline LINEAGE artifact verifies these original signal/entry/event connections and prior P/D fill parity using stored1196/1197 ledgers only. Historical IDs, future exit timestamps, outcomes and period labels in this artifact are never passed to execution. M's frozen logic must calculate new causal admission independently before comparing its finished ledger with the old common-D set. A mismatch is reported and investigated, never forced to the old table. Preregistration prevents new implementation changes; it cannot make the already-known expected result unseen.

## Frozen causal clock

For each symbol, advance one source4h bar at a time. Reuse original finite-prefix EMA20/EMA50 calculation, seed, index239 warmup and original reclaim signals. A flat D reference reserves its first eligible completed-bar signal even when N's close >= (high+low)/2 predicate rejects actual entry. Accepted M entries fill at the original next open. Rejected reservations have no model position, cash, quantity, leverage, fee, funding, PnL or exposure.

While reference is active, subsequent same-symbol signals cannot replace the rejected position. Release only at the original known12-bar timeout close strictly before evaluation end, or the next eligible open after then-observed completed-bar EMA20<=EMA50. A pending prior-bar EMA open exit precedes the current bar's close; on the timeout bar itself native timeout precedes a new close trigger. The exit bar's signal is still owned (i<=exit_index); later bars may reserve a new original D opportunity. No global cooldown or cross-symbol block. The reference remains D, never P, so D-created valid opportunities such as the prior SOL origin can survive.

The clock has no access to _path or a historical D trade list. Only after every chronological admission decision is sealed in the in-memory event stream does the original D batch evaluator supply unchanged actual fills, held geometry and open marks. Those path outputs cannot affect past or later admission. Per-bar reference events carry observation time and local reference signal index computed at reservation, not imported historical trade IDs. A JSON checkpoint stores processed input fingerprints and causal state; exact reapplication is a no-op, changed duplicates/gaps/calendar changes fail. This is an offline research stream, not a new observer/scheduler or operational service.

## Fixed data and economics

Seven existing symbols:1000PEPE/BCH/BTC/ETH/HYPE/LINK/SOL-USDT. Native4h DEV2025 interval[2024-12-19T08Z,2025-12-29T08Z); seen2026 interval[2026-05-08T00Z,2026-09-05T00Z). Original source-prefix causal warmup, fresh-flat evaluation start. Economic row digests match the prior1196 records exactly. The existing bounded reader decodes no row after2026-09-05T00Z and reads no PR1195 observer archive/price/cursor. Original already-seen validation/OOS partition labels are retained honestly; this is reused development, independent=false, not unused validation.

No EMA/half-range/hold/max12/exit/SL/TF/symbol/calendar/quantity parameter changes. The existing Keltner research model has no separate protective SL; M adds none and is not live-safety ready. Model actual positions only with the same nominal amount and common research fee/spread/impact/slippage/funding authority,20bps floor, entry<funding settlement<=exit/mark. Cost2 doubles all cost components. No signed production funding/fill or actual account-sizing claim. Boundary positions remain unfinished with favorable and unfavorable marks and hypothetical full-roundtrip costs; strict-end timeout/EMA pending states cannot be forced closed.

## Frozen comparison purpose and numerical criteria

M-N is the current repair. M-P and M-D expose accumulated gains or hidden damage. Keep inherited min6 closed trades, positive closed net/expectancy, PF>1, realized payoff>=1, cost2net>0; grouped loss-run and daily marked DD;90% capped top-decile reference winner retention;30-day moving blocks,1000resamples,seed1178;1e-7bps comparison tolerance. Strong evidence still requires positive daily-delta95 lower bound and no unresolved positions. SSOT/formal gates are unchanged.

Separate absolute economics from the user-authorized focused repair.2025 recovery requires increased closed net, closed cost2 and hypothetical terminal net, nonworse terminal cost2, grouped loss-run and marked DD, and the inherited large-winner retention floor.2026 preservation requires nonworse closed/terminal net and cost2, grouped loss-run/DD and the same floor; exact equality within the inherited tolerance means UNCHANGED. This is an explicit research interpretation, not relaxed absolute profitability. If2025 recovery and2026 preservation hold, retain M as a partial unadopted development workcopy; do not automatically replace research/operating baselines. Otherwise N remains the unchanged starting workcopy.2026 absolute deficits remain REJECT even if2025 repair succeeds. Unchanged2026 cannot erase a measured2025 improvement.

Report each period P/D/N/M and M-N/M-D/M-P: raw signals/veto/reservations/actual entries, closed/open, gross/net/E/PF/win rate/mean win-loss/payoff/cost2, totalcost/funding, open and terminal values, exposure/max simultaneous symbols, markedDD/grouped loss-run, completed recovery and still-underwater duration, all-reference winning and large-winning amount retention, monthly/symbol concentration and unchanged bootstrap uncertainty. Same-calendar windows attribute common/removed/restored/new effects; overlapping windows and unrelated worst maxima are not additive causal contributions.

Offline2026 residue groups are mutually exclusive gross categories minus costs exactly once, plus separately marked unfinished positions. Report helpful/harmful common exit changes, untriggered-loss and nonloss groups, inherited new-to-P SOL and open marks. The observed untriggered-loss group is an outcome label, not a proposed execution feature. At most one next-cause explanation, no M2 measurement or predictor scan.

## Budget, verification and remote scope

Prior29 candidate history and remaining0 are preserved. Explicit new allocation:M maximum1, ordinal30 only upon actual measurement; two period applications. Existing result reproduction, synthetic tests and historical diagnosis do not consume extra candidates. No alternative candidate, numeric sweep, new indicator, exit retuning, extra TF/period, paid external AI or automatic retries. Gemini actual video NOT_RUN.

Before any M price replay: synthetic causal prefix/suffix, timeout/open priority, exit-bar ownership, rejection reservation/release, cross-symbol independence, strict tail handling, invalid/missing/duplicate/gap chronology, checkpoint idempotence and disabled-N parity. Verify virtual events never contribute model costs/exposure. Freeze code/spec/lineage/design/CI remotely, then actual full M replay twice (one per period), then allowed same-result reproduction. Reuse the existing unsealed Keltner exit-risk CI with path routing; already sealed workflows stay byte-identical. Merge research evidence only after required CI, then reproduce on master.

Q0/B, all Top5 originals and failure records, Q0 prospective code/boundary/schedule/archive and G5B collector/open intent/operating strategy remain untouched. Automated telemetry updates from current master are preserved. execution=NONE/order=BLOCKED/live=BLOCKED; formal_credit0; no actual-account sizing or trading deployment. New unused validation requires separate candidate/data freeze, eligible unused source and authorization; it does not block this approved DEV calculation.
