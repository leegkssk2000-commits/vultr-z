# One C54 incremental entry experiment — before outcomes

User: 저기서 니가 말했듯 추가개선 가능한 부분들 개선 들어가. Direct research parent C54/B from PR1226; preserve C51 profit-zone protection and C54 ATR entry cap. C53/55 overblocking, C52 grace failure and all55/90 prior attempts remain. Latest master and recent PRs checked once at task start; PR1226's two P2 findings were addressed, CI8/8 passed and it merged as a0960fc212453946e83f105fdca5c846738bc6e6. No same-purpose later experiment was found. This Chat is sole budget/economic owner; no second Work launched. Explicit research/tests/relatedCI authorization uses AGENTS exception. No SSH/deployment. Canonical validation runs in the full remote checkout; local package lacks frontend/import-only alpha_proof dependencies, so local integrated QA is NOT claimed. Pure helper2 checks and py_compile passed; full synthetic/regression tests MUST pass remotely before any economic freeze/start.

## Diagnosis and bounded choice
Read only saved C54 positions and original already-used OHLCV. Single diagnostic feature: recovery signal volume / mean immediately preceding contiguous EMA20 pullback volume.2025 median ratio: winners0.9338057192(n83), losses0.9185772121(n108).2026: winners0.8003230617(n26), losses1.0368424230(n43).2026open2 kept separate. Overlap is substantial; these are not independent statistical separation or economic results. Also inspected that one feature on20 C51 origins omitted by C54; no threshold-return table or alternative candidate PnL was calculated. No strategy replay, no body-condition scan.

High-volume confirmation is NOT supported by this diagnostic and is not implemented. Instead test one narrower hypothesis: unusually high participation without corresponding bullish candle-body progress marks a weak reclaim. This is an unproven development hypothesis, not a named-market law, actual order flow, optimized indicator or guaranteed risk reduction. Original C54 gain/concentration limitations remain.

## Exact rule
At the original recovery signal i, use C54's immutable q: last completed close above native EMA20 immediately before the contiguous pullback ending i-1. Compare signal volume to arithmetic mean volume of bars q+1..i-1. Compare max(close_i-open_i,0) to arithmetic mean abs(close-open) over the SAME preceding bars. Reject only when volume is STRICTLY higher AND bullish body is <= that mean. Otherwise keep the original C54 B decision. Missing episode or nonpositive mean volume refuses entry with a separate unavailable reason. No rolling-period constant, calibrated ratio, symbol/year selector or fitted coefficient. Signal is available only at its completed close; same next-open fill; no gap price guarantee. Diagnostic winner labels never reach the rule. Record source bars for independent saved feature arithmetic.

Keep C54's B test unchanged: 0<(signalclose-nativeEMA20)/previousWilderATR14<=1. No revival of failed ADX A/AB, no retuning1ATR, no C52 grace, no new SL/TP, no entry delay, exposure sizing or protection change. Original virtual reservations survive vetoes. Actual occupancy is FULL chronological, not a subset assumption. All raw signals/reasons and open marks retained. Per-process hooks restored even on failure; single serial owner only, no thread parallel replay.

## Budget and comparisons
Maximum1 new hypothesis56, exactly up to2 first FULLs91/92 (DEV2025 then SEEN2026); each real attempt remotely reserved/read back before execution. Number only on actual start. Preserve failed/unknown consumption; no automatic retry or threshold inversion. Exact C51 input artifacts reused; C54/C51/KR3 results reused. No new collection, unused OOS, paid model API, order/live authority or deployment.

Primary cumulative comparison C54→C56; C51/KR3 remain original controls. Eight strict development goals: WR/net/all-cost2 up and dailyDD down in each period. Existing numeric equality precision1e-7/rel1e-11 prevents roundoff-only pass, not new SSOT trading thresholds. Report flat/unchanged separately. Include meanloss, PF/payoff, whole signal denominator, all common/new/removed/open contributions, capped winner/topdecile retention, DD/streak/exposure and largest-origin/symbol dependence. Do not sum overlapping DD windows. Trade-bps are fixed-notional research proxies; no account-return/15x/independent/G5 PASS claim.

## Finish
Code/spec/data/cost frozen before first outcome. Remote synthetic tests before economics; saved raw-price/cost/feature-source/metric/attribution checks afterward, no closing replay. Remove economic dispatcher after the finite attempt(s); read-only final CI/review/merge/exact-merge saved verification. New shortcomings are not permission to extend this candidate budget.45min task ceiling, bounded waits, no repeated reviewers. Real blocker -> exact checkpoint; no completion fabrication or UI/billing-stop guarantee. Other Top5/G5 collectors and unrelated work are untouched.
