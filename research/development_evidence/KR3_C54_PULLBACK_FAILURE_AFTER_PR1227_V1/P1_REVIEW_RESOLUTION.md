# P1 first-breach omission verification

Review3963919866 on d10456c correctly identified a validation gap: the saved check could prove recorded floor intents but did not independently reject an omitted earlier qualifying held close. This follow-up leaves all frozen strategy/SPEC/raw/results/receipts/budget/old manifests unchanged and adds a source-bound assertion oracle, not a strategy candidate or economic replay.

verify_first_breach.py reads the exact ORIGINAL stored input packets (same hashes as C57 SPEC), independently reconstructs the bounded4*n EMA and current research-cost arming condition, and verifies each recorded position's FIRST terminating predicate within its existing held window. It checks both paths with and without recorded floor intents, original exit priority/C51 activation, pending-window semantics and next-open source price. It never creates entries, trade records, alternative exits/PnL, admissions or new source observations. Omitted and delayed signals now fail even when no floor trace is present.

Actual local check:262 positions/2692 held completed closes,29 first floor intents and233 nonfloor paths;0 missing/delayed events, original outcomes unchanged. Eight regressions cover deleted trace, delayed trigger, previously nonfloor path with a qualifying omitted event, false guard arming, source-clock break, stop-line fill fabrication and unused future suffix. Full frontend validation remains the remote-checkout step, not claimed locally.

The supplemental workflow is contents:read/actions:read with no retained credentials or writer. It retrieves ONLY existing artifact kr3-profit-zone-34282003231 from run34282003231 and checks both input byte hashes before reading. This is original-evidence recovery, not market collection or an additional economic run. If that stored artifact is unavailable/expired, the source verification must fail rather than silently substitute data; exact packets are also retained in delivered checkpoint archives. The previous workflow/old evidence remain immutable. This necessary P1 check supersedes any blanket claim that final validation downloads no archived inputs; no exchange/source API is contacted.

Candidate57/evaluations93-94 remain the only two actual executions in this scope. Total57/94. Economic decision REJECT_KEEP_C54 unchanged, new hypotheses/market replays/orders/deployments0 for this review repair. Final PR/merge status is recorded separately.
