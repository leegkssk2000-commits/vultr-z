# Squeeze Continuation bounded sprint: pre-outcome contract

Authority: Issue1267 and uploaded WORK_NEXT. Exact cumulative DEV parent is PR1262 C70_TM_CAPREUSE_V1 candidate82; PR1266 base preserves all84 candidates/152 evaluations. C70_LOCAL reg71 entry is immutable. Profitlock83/lotlock84 are failed references only. Qullamaggie is CONTROL. Every new adapter changes one lifecycle axis, as SOURCE_REGISTRY specifies.

Stage1: eligible slots02/03/04/07 only, up to8 investigated slots. Use saved CAPREUSE campaign entries and allocations exactly. ADD_EXIT only scans saved held observations. REPLACE_RUNNER retains actual partial and scans a fixed-entry exit-only suffix, which can extend after old SMA10 exit; no entry generator, occupancy decisions or full lifecycle replay. Original D3 same-open safety remains. Proxy exposures may overlap above1; no claim of executable portfolio capacity. Both USED_DEV windows must individually pass source/causal integrity, net>0, cost2>0 and ordinary/top-decile retention>=.60. Clipped wins and extra costs count in total incremental. Missing cohort retention is not a pass. If more than3 pass, per-window cost2, ordinary/top10 retention, loss-tail and negative exposure Pareto vector narrows; then grade A>B>C, summed cost2 descending, slot lexicographic ties. Never a weighted optimization score.

Stage2: only Stage1 survivors, max3 registered canonical candidates and6 first FULL. Same exact rule in both sealed USED_DEV windows, parent replay0. Preserve parent SPEC cumulative workcopy criterion: both net/cost2 improve, DD not worse, ordinary/top-decile retention1 (floating accounting tolerance1e-12). Require closed-net gain and no worse worst-loss/decile loss tail. This is not relaxation to Stage1's60%. Any failed window blocks adoption; positive net with risk/retention failure is TRADEOFF, otherwise REJECT. Max2 survivors using same predeclared per-window Pareto/tie mechanism.

Stage3: no survivors => exact CAPREUSE freeze. One survivor => existing child identity, FULL0. Two same-axis => deterministic best single. Two orthogonal => B1 existing best single and only one new B2, FULL2. Bundle must pass the same both-window gate, beat best single normal/cost2 in both windows, and not worsen DD, winner retention or occupancy loss. Interaction = (bundle-parent)-(A-parent)-(B-parent), separately normal/cost2. Negative normal or cost2 interaction rejects bundle. No combination/parameter sweep.

Challenge sealed NOT_AVAILABLE before Stage1: no new historical window discovery. Challenge FULL0. If available it would take priority over optional bundle within total10; current unavailable reservation0. All DEV/screen/challenge formal credit0.

Hard caps: source slots8; Stage1 eligibleitems4, FULL0, canonical0; Stage2 candidates3/FULL6; Stage3 bundle1/FULL2; challenge0; total canonical4/FULL10. All failed starts consume their budget; retries0. Parent/C63/Top5 replay0. FIXED, paid API, live, orders, deploy, arbitrary OOS, parameter sweeps0. Root sole economic writer.

After outcomes, always freeze one Squeeze Continuation v1 exact identity, publish G5B PREPARED handoff with explicit unmet activation prerequisites, then CI/review/normal merge/exact saved verification and REPORT_ONLY close only this DEV scope. No fresh formal activation in this issue. T6 diagnostic, T12 provisional, terminal explicit; G6 only after same-lane G5B terminal PASS. Later ideas go to v2_backlog and cannot extend this sprint.
