# Squeeze-KR3 Unified v2 — pre-outcome design

One candidate only:

`U4 = immutable CAPREUSE82 core OR exact C54 actual native donor campaign when same-symbol core is fully flat`

This is the direct TrendRider-style core+donor architecture. It is not PR1293's filter/exit graft.

## Core priority
- Every saved CAPREUSE campaign row is retained byte/economically unchanged.
- Donor full-size entry is allowed only at its original entry open if no same-symbol CAPREUSE campaign is active and no accepted donor is active.
- If a CAPREUSE core entry later occurs while donor is active, donor exits at that exact observable core-entry open immediately before core entry (`CAPREUSE_CORE_PRIORITY_PREEMPT`).
- Natural donor exit at the same timestamp precedes core entry.
- No delayed donor entry and no fractional donor use of CAPREUSE spare capacity.
- Timestamp event ordering, not future overlap screening, implements arbitration.

## Donor source
Exact saved C54/B campaigns only (`KR3_C51_ENTRY_CONTEXT_B_V1`). Counterfactual C54 opportunities excluded by its original standalone occupancy are not regenerated.

## Budget / selection
Candidate89; eval160 DEV2025; eval161 SEEN2026. Two first FULL union simulations, no retry.

Eligibility: core retention100%, causal/accounting clean, each-period net>0/cost2>0/PF>=1, donor accepted>=1. Adopt iff two-period combined terminal net is strictly greater than CAPREUSE. WR decline alone is not a hard reject.

USED_DEV/formal_credit0. No fresh/Q/G5, new market/OOS, paid AI, orders or deployment.
