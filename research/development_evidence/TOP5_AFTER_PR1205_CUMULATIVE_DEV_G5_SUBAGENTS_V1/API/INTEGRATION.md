S3 delivers `api_pilot_runtime_v2.py` and its free regression suite. Existing pilot routes, redirect rejection, Registry reservation/finish state transitions and both old source modules remain unchanged. No provider request was made.

The root integration owner must seed ONE canonical `API/SHARED_TASK.json` on master from the preserved old TASK plus current TASK, in `research.scope.v1` schema. Store exact actual completed parent TASK bytes in new-scope `API/INHERITED_TASK.json` (the old Git TASK is a stale pre-completion snapshot; root has the actual completed source). Set top-level `budget_scope` to `TOP5_LIFECYCLE_AI_G5_AFTER_PR1204` and `inherited_task_sha256` to SHA256 of those exact inherited snapshot bytes; preserve old task bytes semantically and the current actual counters/attempts. Current W2 must be open for a necessary request. This is an inherited view of the existing Registry, not a new budget. Do not create any separate per-provider/subagent ledger. The adapter cannot initialize files, choose another repository/path/branch, or reset counters. Root alone owns initialization and reconciliation.

GitHub Contents GET returns file blob SHA; PUT requires that SHA and the fixed branch. After PUT, the adapter reads back exact data and returned blob SHA. Concurrent stale SHA conflicts fail without provider network. A lost PUT reply may have committed: preserve that reservation and stop, without resending. A provider timeout, transport error, parse error, or response with usage but no authoritative charge keeps billing UNKNOWN and the full reservation; the other provider is blocked until authorized reconciliation. Both scopes together remain <=USD5, Gemini<=1, OpenAI<=1. Implicit input caching is possible; no explicit cache storage is created and the full uncached price is reserved.

Proposed root-only separate workflow: `.github/workflows/top5-cumulative-api-v4.yml`, `on: workflow_dispatch` only; `allow_network` boolean default false and `approval_file` string default new-scope `API/API_APPROVAL.json`. No push/PR/schedule trigger. Workflow concurrency constant `top5-shared-ai-budget-pr1204-pr1205`, cancel false. A read-only `probe` job binds existing GEMINI_API_KEY/OPENAI_API_KEY secrets and runs `python -m backend.research.rebuild.api_pilot_runtime_v2 --no-network`; output records only presence. A separate `pilot` job requires manual event, allow_network true, `github.run_attempt == 1`, timeout 3 minutes, `permissions: contents: write`, GITHUB_TOKEN=${{ github.token }}, existing provider secrets, and APPROVAL_FILE=${{ inputs.approval_file }}. Checkout persist-credentials false. Run `python -m backend.research.rebuild.api_pilot_runtime_v2 --approval "$APPROVAL_FILE"`; always upload `out/top5-cumulative-api-v4.json`. This is a proposal for the root writer; no workflow file was edited by S3. Do not use rerun tools to initiate a paid action.

Approval must bind `inherited_task_sha256` plus exact current scope/id/provider/model/dossier SHA/provider quote SHA/runtime commit, explicit manual approval, actual unresolved-question need, and tax/FX upper bound plus its billing authority. Freeze code first, then commit the approval referencing that code SHA. Checkout the later approval commit with fetch-depth 0. Runtime verifies HEAD equals the event SHA, the approved code SHA is an ancestor, and all three executed pilot/Registry owner modules are byte-identical to that earlier code commit. This avoids a self-referential commit hash while binding actual executing bytes. Dossier and quote remain separately approved by exact hash. A code edit after approval requires a new reviewed approval, without any budget reset.

Official evidence retrieved 2026-09-07:

* [Google standard pricing](https://ai.google.dev/gemini-api/docs/pricing#gemini-2.5-flash): text input USD0.30/M and output including thinking USD2.50/M. [Model](https://ai.google.dev/gemini-api/docs/models/gemini-2.5-flash): accepted input limit 1,048,576; output 65,536. [Developer reference](https://ai.google.dev/api/generate-content?hl=en) describes `thinkingBudget` as “The number of thoughts tokens that the model should generate.” Retrieved current thinking guide does not prove a hard exact-route combined cap; flag remains false. The different Vertex host's disable-thinking example is supporting evidence only.
* [OpenAI model](https://developers.openai.com/api/docs/models/gpt-4.1-mini): gpt-4.1-mini-2025-04-14, context 1,047,576, max output 32,768, input USD0.40/M and output USD1.60/M. [Output limit](https://developers.openai.com/api/docs/guides/reasoning) includes reasoning, visible output and non-visible formatting tokens. The adapter sets `max_output_tokens=6000`. Conservative full-context input plus capped output is USD0.4286304 before any tax/FX. Full-context reservation avoids claiming UTF-8 bytes are tokenizer proof.
* [GitHub Contents](https://docs.github.com/en/rest/repos/contents#create-or-update-file-contents): SHA is required for replacement; conflicts prevent concurrent overwrite. Endpoint access still requires actual repository contents-write permission; static workflow YAML is not runtime proof.

Actual connection blockers are recorded in RUNTIME_EVIDENCE.json. Local provider/GitHub token absence was observed; remote secret absence was not. No manual dispatch or secret-presence capability is advertised in this Work tool set. Needed binding is one first manual workflow_dispatch on the approved repository/code, existing provider secret presence, a root-seeded canonical ledger with usable contents-write token, reviewed invoice/tax/FX upper bound, and exact request semantics for the selected provider. If S1/S5 already resolve the TPC1 question, root should record zero calls because the question is solved. Never send old completed TPP1 justification as a new paid request.
