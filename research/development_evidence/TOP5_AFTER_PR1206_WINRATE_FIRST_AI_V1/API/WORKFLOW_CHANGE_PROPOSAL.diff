--- a/.github/workflows/top5-cumulative-api-v4.yml
+++ b/.github/workflows/top5-cumulative-api-v4.yml
@@
       - name: Exact manual approval with committed shared reservation
-        run: python -m backend.research.rebuild.api_pilot_runtime_v2 --approval "$APPROVAL_FILE"
+        run: |
+          case "$APPROVAL_FILE" in
+            research/development_evidence/TOP5_AFTER_PR1206_WINRATE_FIRST_AI_V1/API/API_APPROVAL.json)
+              python -m backend.research.rebuild.api_winrate_runtime_v1 --approval "$APPROVAL_FILE" ;;
+            research/development_evidence/TOP5_AFTER_PR1205_CUMULATIVE_DEV_G5_SUBAGENTS_V1/API/API_APPROVAL.json)
+              python -m backend.research.rebuild.api_pilot_runtime_v2 --approval "$APPROVAL_FILE" ;;
+            *) echo 'APPROVAL_PATH_OUTSIDE_ALLOWED_SCOPES'; exit 1 ;;
+          esac
@@
           name: top5-cumulative-api-pilot-${{ github.run_id }}
-          path: out/top5-cumulative-api-v4.json
+          path: |
+            out/top5-cumulative-api-v4.json
+            out/top5-winrate-api-v5.json
