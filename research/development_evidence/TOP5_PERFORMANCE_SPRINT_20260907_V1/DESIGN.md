# KR1 and QF1 joint preregistration

User authorization: `ZEL_TOP5_PERFORMANCE_SPRINT_KELTNER_Q0_WORK(1).txt`, all 235 lines read. Base master `78c0bce3029b1a11dc821dd72fefd6297f8b7255`; no same-purpose PR, running economic process, or completed receipt found before implementation. No qualification rerun. Both rules, code, input SHA, decision owners and budget freeze together before any new market-path application. Synthetic tests do not consume trials.

KR1 is a development child of PR1200 M2, never an adopted incumbent. Preserve M, M1 REJECT and M2 2025 TRADEOFF / 2026 REJECT. The exact rule is duplicated as structured `SPEC.rules.KR1` and bound to the adapter SHA. For original signal i, H=12 and t=i+H, process original pending D/M2 orders first. At t-1 completed close, if alive and close > actual entry price and close > EMA20 > EMA50, permit exactly one additional H. Otherwise keep original t CLOSE timeout. From t, close <= EMA20 queues the next actual open strictly before the calendar end. D > allowed M2 > runner priority. First-low-breach SUPPRESSED stays suppressed. At i+24, planned CLOSE timeout has inherited priority and strict-end semantics. An unavailable fill remains censored and receives hypothetical full-cost terminal valuation.

The unchanged causal D clock determines the original M2 eligible opportunities, including virtual reservations for rejected/blocked signals. FULL admits only this set when the actual position is absent. `signal_ts <= actual_exit_ts` is blocked: at an identical close/open timestamp the old position owns the signal. A later completed close in the same bar as an exit-open can use the now-free slot, provided the original reference clock permits it. D reference exit-bar ownership stays unchanged. The inherited deterministic batch path supplies an exit timestamp; no future price, range or profit is used for admission. Prefix tests prove earlier admissions persist when a later exit is censored. Restart means deterministic full actual replay plus resumable/idempotent reference checkpoint, not a new live runner service. FIXED evaluates each original M2 entry independently and can overlap; it is a path diagnostic. FULL is the candidate's executable model sequence.

QF1 is a development child of Q0 A, preparation=true and unit notional. Generate the original UP/DOWN attempts completely unchanged. At original UP confirmation, compare the original anchor daily close (`anchor_daily_index`) with the original second confirmation close. Admit only strictly positive progress; ties and negative progress reject only that UP. Rejected confirmations remain in the opportunity denominator. The original generator never restarts an attempt because of this filter. DOWN, original lower SL, gap priorities, next-open admission/cancellation and original occupancy remain unchanged. FIXED uses the parent's admitted UP opportunities with all original DOWN signals. FULL filters all original signals and replays its own occupancy; new replacement entries are separately attributed. No Keltner reservation or B allocation is imported.

Both design choices (one additional H and zero-direction progress) are this user's DEV assumptions, not optimized thresholds, established profits or official gates. No sweep, extra symbol, extra date, alternate EMA, later source suffix, KR2 or QF2 is authorized.

Use the seven parent symbols, bounded original source prefix, 239-index EMA warmup where applicable, unchanged fees/spread/impact/slippage/funding, 20bps floor and full-component cost2. Keltner DEV2025 is [1734595200000,1766995200000); Q0 DEV2025 is [1738108800000,1766966400000). Both reused SEEN2026 are [1778198400000,1788566400000). Decode only the old bounded loader's 3748 rows through 2026-09-05T00Z; opaque file SHA checks do not decode excluded suffixes. Exact parent ledgers are reused; Q0 stage/daily valuation is rebuilt from its sealed fills for the common reporting API, not a signal experiment. Q0 baseline receipts are explicitly bound in SPEC.

Judgment owners: KR1 uses unchanged `parallel_exit_metrics_v1.compare`, as M2. QF1 uses unchanged `keltner_cumulative_entry_metrics_v1.compare`, the existing Top5 ENTRY_FILTER version of the same PR1196 numerical criteria. Its legacy GOAL narrative names N/P/D; it is retained verbatim for traceability while this study maps base=Q0 and child=QF1, in both FIXED and FULL. No exposure-reduction-specific Q1 or notional-allocation B goal is silently substituted. Report all existing failed checks, closed absolute economics, full terminal base/cost2, ordinary and top-decile winner retention, grouped losses, daily DD/recovery/exposure, 30-day/1000-resample/seed1178 uncertainty and event/month/symbol concentration. Preserve partial favorable effects even when an aggregate candidate is REJECT. No automatic incumbent or workcopy replacement.

Attribution uses symmetric common/absent/new/closed/open origins. Absence is zero contribution only, never a zero-return trade or win. All terminal value components obey (FIXED-parent)+(FULL-FIXED)=FULL-parent. Report missed original net separately from missed candidate-path net. Funding follows each actual modeled holding interval. Already-net cost savings are never added twice. All winner-to-loss origins remain in artifacts.

One economic writer and at most one read-only reviewer. The first real application creates immutable ATTEMPT before feature/path evaluation, consuming the next ordinal after 32 even if it fails. Two candidates maximum. A pre-input-integrity failure consumes no unexecuted candidate. All later runs are explicit verify-only against committed immutable receipts. Separate old ledgers remain independent 0/1 NOT_RUN, seen evaluation 1/1, qualification extra 0/1. Future Q0 observer code/SPEC/boundary/collector and G5B stay byte-preserved. Paid AI and orders remain zero.

The sprint-only CI is necessary because the prior M2 workflow itself is sealed. It verifies synthetic dependencies and only reproduces committed sprint receipts, serially. It does not rerun old economic experiments, dispatch market collection, deploy, generate new candidates, or invoke qualification. Complete with one minimal PR, relevant CI, merge and exact master reproduction, then stop. At 45 minutes record the current safe checkpoint and actual run IDs; if no code/log/remote progress for 15 minutes inspect the last real run. No infinite CI polling or automatic next-candidate budget.
