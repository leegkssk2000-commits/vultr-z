# Pre-outcome operational choices

Authority: attached WORK_NEXT / Issue1250, one JC candidate, two first FULLs.
The attached A01-A12 remain the rule. Carter's actual options/account performance is not reproduced. All quantitative pattern rules are ZEL choices.

- Daily squeeze calls the unchanged canonical predicate with only timestamps normalized, then maps decisions back to completed UTC days. EMA8/21 SMA-seeded conventional alpha 2/(n+1); ATR21 Wilder seed after 21 true ranges.
- The latest alternating confirmed extrema replace an immediately repeated same-kind pivot with the latest one. A daily bar that qualifies as both high and low is not ordered artificially and is excluded as a pivot. Three squeeze days must follow handle confirmation. Equality counts as a rolling-high event; strict radius2 pivots retain strict inequalities.
- Prices/chronology stay in original 4h coordinates. New setup/orders at a daily closing timestamp act only on the next observed bar, never its predecessor. No initial carried orders/positions. An occupied setup is logged without releasing capacity. A used handle cannot rearm.
- Source warmup GETs were seven single attempts. Responses include a candle spanning the 2024-12-19 08:00 evaluation start; those seven rows are quarantined. Complete prior days can be normalized from the original saved bytes without retry. Original packet lacks 00:00 and04:00 on its first day. No daily interpolation: reset the rolling history to the latest contiguous completed UTC daily segment (2024-12-20). DEV2025 early dates are ineligible until365 complete days. SEEN2026's bound prefix already includes sufficient history. No younger-listing all-time alternative without verified listing origin.
- Native decimal price units come from preserved PR1209 raw contracts SHA1f0330a65e371ab95aea9c37c1e690f43465339377b714e3fd1c841936eb222f:10^-pricePrecision. This2026-09-07 snapshot is a fixed research precision assumption, not historical precision-change evidence or live order authority. Fractional reference quantities are not exchange-lot fills.
- Conditional execution uses the established Q0 observed-open gap/touched-level convention. For each bar the strategy-specific state machine evaluates feasible O-L-H-C and O-H-L-C ordering and retains the lower closing cost-aware liquidation value, logging both values, chosen path and allocation. This local OHLC ambiguity model is not tick reconstruction or another FULL trial. Equal-level protective exits precede adding exposure; equal EMA limits both fill their allocated shares. Gap breakout consumes remaining budget before unused limits. Intrabar fill timestamps use bar-close upper bounds; 72h starts at that conservative modeled first fill timestamp, not a claimed tick time.
- Stops after added fills ratchet immediately from actual weighted entry without widening. Profit-triggered protection is deferred to the next observed bar. A first profit snapshots assembled actual quantity and sells one third at each first two levels. Every sale apportions remaining lots pro rata; lot entry notional weights sum to the single invested reference budget. Invalid risk geometry cancels unfilled orders. Final runner trails three completed daily lows, effective next bar.
- Cost uses unchanged per-symbol research fee/spread/impact/funding settlement proxy with existing20bps unit floor. Each lot/sale portion pays its fraction of one roundtrip, avoiding duplicated entry charges. Whole campaigns define WR; partially realized unfinished campaigns retain realized+remaining hypothetical net. Daily DD values weighted actual lots with the existing boundary owner, restoring the temporary hook on exit/error.
- Promotion uses unchanged m1_er14_study_v1.checks (WR/net/cost2/DD in each window), no special exception for zero trades or limited coverage. IndependentOOS/G5/account return/live promotion credit=0.
