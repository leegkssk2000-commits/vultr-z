# STEP7 S2 candidate application implementation

The exact selected KR3 FULL candidate is linked to the previously missing entry and exit identities. Entry is the preserved `keltner_kr1_v1.py:replay` ownership executor; exit is `keltner_kr3_v1.py:path`. Both complete file bytes are checked against the historical SPEC pins, and each entrypoint has a function AST digest. The complete historical SPEC bytes retain the original dependency/configuration pins. This extends the identity record without changing the immutable selection or strategy.

KR3 retains no protective SL and no TP. Its native/extended caps are observed bar offsets 12/24 from the signal, not a wall-clock guarantee across gaps. Final cap closes precede other same-bar condition handling. Other permitted completed-close exits use the next observed open. Q0 has a separate profile: latched lower-channel SL, no time cap, gap/bearish/intrabar priority, and intrabar timestamp as an upper bound rather than an actual fill. Both profiles reject the TIME_STOP_ONLY substitution.

The new producer contract binds all nine required report kinds to candidate/data/cost/preregistration digests, executable function bytes, exact argv and evaluation-spec digest before execution. The result adapter checks exact completion, all receipt identities, executed-source receipt reference and actual artifact bytes; a PASS string, substituted cost hash, missing payload or synthetic evidence is rejected. This is byte/identity binding only, not source authenticity or economic approval. The existing Alpha Proof and G5B owners remain unchanged.

The actual application command used the selected candidate metadata, the preserved result receipt and prior DEV binding report. It did not open the 203-row artifact or repeat the 1,469-check audit or the previous seven tests. Existing stored DEV producer pointers are reused. Independent producers have not been preregistered/executed and remain incomplete; no report is declared formal complete by this application.

## Actual result

- application state: IMPLEMENTED_APPLICATION_BLOCKED_BEFORE_INDEPENDENT_EXECUTION
- new metadata connection: exact FULL candidate, entry SHA, exit SHA, conditional native profiles and nine-report producer/consumer schema
- new market replays: 0; independent data accesses: 0; formal credit: 0
- prior verdict: TRADEOFF preserved; G5A qualified false; G5B terminal false
- actual sources still required: signed funding, entry/exit quotes, availability/cursor, independent data provenance
- authority/preregistration owner: S4; no new formal numeric values are activated by this module
- remaining execution: approved producer specs, one independent bundle, existing P0–P6/G5A consumer, then fresh formal cohort only if eligible

Eight new synthetic regression tests passed in 0.009 seconds. Coverage: exact native meaning and no SL invention, Q0 no timeout, missing execution, false report strings, resealed cost substitution, byte corruption, all-nine binding still not economic PASS, synthetic/actual evidence distinction. No market replay occurred. The repository-wide frontend postcheck is root-owned, so S2 did not repeat it. No deployment should be run.

## Reproduction command

```sh
python -m backend.research.rebuild.step7_candidate_contract_v1 --selection research/development_evidence/ZEL_PARENT_CLOSURE_TO_G5_SURVIVOR/SELECTION.json --output research/development_evidence/ZEL_PARENT_CLOSURE_TO_G5_SURVIVOR/CONTRACT/APPLICATION.json --verify
```

Rollback is removal of the two new modules and this candidate-local directory; existing strategy/gate/source owners were not modified. No service or order was activated.
