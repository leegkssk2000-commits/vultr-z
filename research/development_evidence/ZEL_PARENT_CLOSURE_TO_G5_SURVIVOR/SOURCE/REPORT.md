# STEP7 canonical source / availability connector

Status: CODE_READY_ACTIONS_SOURCE_BUNDLE_PENDING. No public market response was received in scratch; formal T = 0. Source code and synthetic PASS do not mean collection is running.

The existing caller remains `g5_forward_real_evidence_bridge_v4` under `g5-forward-real-evidence-v1.yml`; it has TIME_STOP_ONLY semantics. Existing bridge state was inspected only for cursor/shape/time metadata. No price, position, or result values were emitted. Its raw canonical cursor was absent. Existing active V2, Q0/G5B observations and services were not changed.

Official support for the four public endpoints is listed in [BingX API rate-limit notice](https://bingx.com/pt/support/articles/31103871611289); current docs are [BingX API Docs V3](https://bingx-api.github.io/docs-v3/). These are public depth, native 4h OHLCV, signed funding-rate history and contract information endpoints. No account or order endpoint is used.

The root owns the single authorized fixed-merge GitHub Actions source bundle: The frozen SELECTION.json KR3 seven-symbol universe, 22 GETs maximum (depth/OHLCV/funding per symbol; contracts once), no retries/fallback, first failure ends the bundle. OHLCV requests 240 bars for later seed/warmup binding. The source manifest must exactly match the existing Clean Runner source universe; snapshot collection does not establish seed or strategy parity. Historical overlap independence is UNKNOWN and all preboundary observations have formal credit zero. The code does not evaluate candidate economics, compute signals, backfill historical quotes, or turn depth into actual fills.

Each actual HTTP response will be stored verbatim, with request/receipt time, endpoint URI, run ID and SHA; parsed records preserve exchange timestamps only when supplied. Availability is receipt time, never a retroactive OHLC close. Funding signs remain unchanged; settlement completeness needs a position interval. Book sequence continuity is unproven for a snapshot. Stream-plus-symbol SHA cursor stores reject duplicate candidate collection, preserve restart gaps in quarantine, and protect one-bundle restart with a file lock plus ATTEMPT guard. Root must retain this attempt artifact/cumulative budget across clean CI runners; a new filesystem alone is not a campaign budget reset.

Q0 lacks an explicit LAST/MARK/INDEX trigger binding and remains TRIGGER_STREAM_UNBOUND. KR3 completed-bar/native-next-open reference rejects unavailable decision data, backdated native opens and arbitrary later replacement prices. It does not create trades or charge costs. Source-specific stale authority must be supplied; no 4h stale threshold was copied to a tick.

Validation: 10 new synthetic tests passed initially; the bounded seven-symbol follow-up added and ran only 2 new manifest/cross-symbol cursor tests (PASS). Existing 10 were not repeated. They cover completed-bar availability, duplicate conflict/missing quarantine, stored parse parity, restart cursor reuse, signed rates, shared canonical references, unbound/future/stale rejection, one failed attempt with no retry, raw preservation on parse error and native next-open causality. Frontend validation passed. No old economic replay or old arithmetic audit was run.

Scratch documentation retrieval encountered `network approval was cancelled before a decision was returned`. No scratch public-market HTTP was then attempted and no approval bypass was used. The root will perform the separately authorized existing-environment bounded Actions call, or record its exact failure. It must report actual source reception separately from code readiness. No new daemon, background service or post-session collector was created. Actions deploy is not needed.
