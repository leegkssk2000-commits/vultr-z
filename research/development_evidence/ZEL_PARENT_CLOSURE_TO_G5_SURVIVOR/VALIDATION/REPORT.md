# STEP7 independent validation guard

16 distinct synthetic tests passed:11 initial guard cases,4 added producer cases,and1 cost2 integrity regression, without repeating completed cases. No market/holdout files or economic results were read, and no API or economic replay ran.

Implemented pure prerequisites, exact identity and authority digest binding, developer/validator split, nine-report manifest completeness, fixed W1-W3 review, unknown-use refusal, one inherited access reservation, and tamper/concurrency refusal. Parent sole writer must persist the returned reservation before IO; this is not an OS access-control boundary or user-authentication service. The guard does not generate any G5A/G5B PASS.

KR3 no-SL statistical R, numerical sample/error/window/source/risk proposals are PENDING_AUTHORITY. G5A prospective data remain HOLDOUT_SEALED with formal credit0; they cannot become G5B fresh rows. Exact six-neighbor tuple inventory is frozen as a PENDING_AUTHORITY proposal; native source enforces PARENT_SPEC, so an isolated sensitivity replay producer remains required. All formal controls and neighbor results must share the same sealed candidate/data/cost identity; existing DEV economic results cannot substitute. Existing shared thresholds and KR3 TRADEOFF remain unchanged.

The development author of a validation guard may not serve as the independent economic executor. Assign a distinct operator with no proposal role before sealed access.

The supplied sealed-ledger producer generated SYNTHETIC_DERIVED_REPORT.json from SYNTHETIC_INPUT_LEDGER.json and SYNTHETIC_SPECIFICATION.json. It reused the existing summary owner to compute actual synthetic W1 net10/cost2net8 and W2 net-7/cost2net-9, purged future/overlapping/reference/open training intervals, and preserved two W3 unresolved/late origins without importing the synthetic late +998 profit. These are synthetic counterexamples, not market economics.

Cost2 integrity fix: reject supplied cost2x_net_bps unless it equals gross_bps minus twice cost_bps. A newly resealed ledger that omits or double charges cost now fails before summarization. Only the new regression was run; the existing synthetic derived artifact and all earlier completed checks were preserved without re-execution. Correct-row summary formulas remain unchanged.
